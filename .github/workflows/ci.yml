name: "CI"
on: push

jobs:
  build:
    name: "Run Checks"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: denoland/setup-deno@v2
      - uses: biomejs/setup-biome@v2
      - run: biome ci
      - run: deno check
      - run: deno test
